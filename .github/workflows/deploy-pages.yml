name: Deploy Portfolio to GitHub Pages

on:
  workflow_dispatch:

permissions:
  contents: write
  pages: write
  id-token: write

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Manual deployment is temporarily paused during the atomic V7 release."
